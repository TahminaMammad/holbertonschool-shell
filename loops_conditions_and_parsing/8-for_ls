#!/usr/bin/env bash
# This script lists files and prints the part after the first dash

for file in *; do
    # Skip if no visible files match
    [ "$file" = "*" ] && exit 0

    # Remove everything up to the first '-'
    stripped="${file#*-}"

    echo "$stripped"
done
